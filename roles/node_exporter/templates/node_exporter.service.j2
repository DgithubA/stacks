[Unit]
Description=Prometheus Node Exporter
After=network.target

[Service]
User={{ node_exporter_user }}
Group={{ node_exporter_user }}
Type=simple
ExecStart={{ node_exporter_executable_path }} {{ node_exporter_flags | join(' ') }}
Restart=always

[Install]
WantedBy=multi-user.target
